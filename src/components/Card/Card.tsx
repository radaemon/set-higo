import React from 'react';

import './styles.scss';

type Props = {
  color: Number;
  shape: Number;
  texture: Number;
  quantity: Number;
};

const Card = ({ color, shape, texture, quantity }: Props) => <></>;

export default Card;
