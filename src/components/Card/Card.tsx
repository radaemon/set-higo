import React, { FC } from 'react';

import './styles.scss';

interface Props {
  color: Number;
  shape: Number;
  texture: Number;
  quantity: Number;
}

const Card: FC<Props> = () => <></>;

export default Card;
